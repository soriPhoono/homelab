version: "3.8"
services:
  web:
    image: nginx:latest
    ports:
      - "8080:80"
    restart: always
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
    volumes:
      - ./html:/usr/share/nginx/html
    networks:
      - webnet
networks:
  webnet:
