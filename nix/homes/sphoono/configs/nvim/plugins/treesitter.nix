_: {
  vim.treesitter = {
    enable = true;

    addDefaultGrammars = true;

    context = {
      enable = true;
    };

    indent = {
      enable = true;
    };

    textobjects = {
      enable = true;
    };
  };
}
