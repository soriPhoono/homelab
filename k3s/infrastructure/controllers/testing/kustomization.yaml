# Cluster "testing": k3d/local overlay — no Longhorn; PVCs use k3s/k3d rancher.io/local-path (hostDir binds).
# Keep HelmRelease versions aligned with ../guenivir when you bump charts.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
  - cert-manager.yaml
  - external-secrets.yaml
  - netbird-kubernetes-operator.yaml
  - sealed-secrets.yaml
  - vault.yaml
