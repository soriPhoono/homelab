# Same intent as configs/guenivir (copied here so Kustomize load restrictions allow Flux builds).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
  - clusterissuer-selfsigned.yaml
  - clusterissuer-letsencrypt-staging.yaml
  - netbird-mgmt-api-key.sealedsecret.yaml
